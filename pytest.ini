[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

markers =
    slow: marks long-running / performance tests
    plot: test generates and saves plots (optional, non-critical)